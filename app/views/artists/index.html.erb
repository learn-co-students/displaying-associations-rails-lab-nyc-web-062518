<h1>ALL OF THE ARTISTS</h1>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <%= link_to artist.name, artist_path(artist) %> | <%= pluralize(artist.song_count, "songs") %>
    </li>
  <% end %>
</ul>
